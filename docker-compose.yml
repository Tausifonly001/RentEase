services:
  app:
    build: .
    ports:
      - "8080:80"
    environment:
      - APP_URL=http://localhost:8080/
      - APP_ENV=production
    env_file:
      - .env
    volumes:
      - ./backend/storage:/var/www/html/backend/storage
    restart: unless-stopped
